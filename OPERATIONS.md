# 운영 가이드

혼자서도 이 자동화를 유지·수정할 수 있도록 정리한 문서입니다.
개발 도구(Claude Code 등) 구독과 **무관하게** 파이프라인은 계속 돌아갑니다.

---

## 1. 이 자동화는 무엇에 의존하는가

| 역할 | 서비스 | 비용 | 끊기면 생기는 일 |
| --- | --- | --- | --- |
| 대본·캡션 생성 | Google Gemini (`@google/genai`) | 무료 등급 | 게시 전체 실패 |
| 나레이션 | edge-tts (Microsoft) → 실패 시 Google 번역 무료 TTS로 자동 전환 | 무료, 키 불필요 | 둘 다 실패해야 게시 전체 실패 (아래 9장) |
| 배경 자료화면 | Pexels | 무료 (200/시간, 20,000/월) | 검정 배경으로 폴백, 게시는 됨 |
| 영상 렌더 | Remotion + GitHub Actions | 공개 저장소라 실행 시간 무제한 | 게시 전체 실패 |
| 인스타 게시 | Instagram Graph API (버전 만료 시 자동으로 다음 버전 탐색) | 무료 | 인스타만 실패, 유튜브는 진행 |
| 유튜브 게시 | YouTube Data API v3 | 무료 | 유튜브만 실패, 인스타는 진행 |

**AI 코딩 도구는 런타임에 전혀 관여하지 않습니다.** 코드를 짜고 고칠 때만 쓰였습니다.
소스 어디에도 Anthropic/Claude API 호출은 없습니다.

---

## 2. 워크플로 지도

`.github/workflows/` — GitHub 웹 → **Actions** 탭에서 실행·확인합니다.

### 자동으로 도는 것

| 파일 | 이름 | 일정 | 하는 일 |
| --- | --- | --- | --- |
| `post-reel.yml` | 미스터리 릴스 자동 게시 | 매일 3회 (UTC 01·06·10시 = KST 10·15·19시) + 0~120분 랜덤 지연 | **핵심.** 대본 생성 → 렌더 → 인스타·유튜브 게시 |
| `refresh-token.yml` | 인스타 토큰 자동 갱신 | 매주 월 03:00 UTC | 60일 만료 전에 재교환하고 시크릿 자동 갱신 |
| `backfill-captions.yml` | 기존 영상 자동자막 끄기 | 매일 20:40 UTC | 과거 영상 4개씩 처리. **다 끝나면 스스로 비활성화됨** |
| `health-check.yml` | 🩺 자동화 건강 점검 | 매주 일 22:00 UTC | GH_PAT 만료·게시 중단·유튜브 리프레시 토큰 감시 + 하트비트 커밋. 문제 시 실패로 메일 알림 |

랜덤 지연은 매일 게시 시각을 다르게 만들어 자동화 티를 줄이려는 장치입니다.
GitHub 스케줄러 자체가 1~4시간 늦는 일이 흔하니, 정시에 안 올라와도 정상입니다.

### 필요할 때 직접 누르는 것 (Actions 탭 → 워크플로 선택 → `Run workflow`)

| 파일 | 이름 | 쓰는 때 |
| --- | --- | --- |
| `pause.yml` | ⏸️ 자동 게시 멈추기 | 여행·점검 등으로 잠시 멈출 때 |
| `resume.yml` | ▶️ 자동 게시 다시 시작 | 위를 되돌릴 때 |
| `stats-report.yml` | 📊 조회수·반응 리포트 | 성과를 한눈에 보고 싶을 때 |
| `set-thumbnail.yml` | 🖼️ 유튜브 썸네일 교체 | 특정 영상 썸네일만 다시 지정할 때 |
| `fix-published-text.yml` | 🛡️ 기존 영상 연령제한 표현 수정 | 이미 올라간 영상의 제목·설명을 일괄 수정할 때 |

`post-reel.yml`도 수동 실행됩니다. 입력값:

- `dry_run` = **true** → 게시하지 않고 영상만 만들어 Artifacts 에 올림 (안전한 테스트)
- `seed` → 소재 힌트 (비워두면 알아서 고름)
- `angle` → 소재 각도 강제 지정 (`불륜`, `유산`, `그 시절` 등)
- `skip_youtube` → 인스타만 테스트

---

## 3. 시크릿과 변수

**Settings → Secrets and variables → Actions** 에서 관리합니다.
값 자체는 이 문서나 코드에 절대 적지 마세요.

### Secrets (민감값)

| 이름 | 발급처 | 만료 |
| --- | --- | --- |
| `GEMINI_API_KEY` | Google AI Studio | 없음 |
| `PEXELS_API_KEY` | pexels.com/api | 없음 |
| `IG_USER_ID` | 인스타 비즈니스 계정 ID | 없음 |
| `IG_ACCESS_TOKEN` | Facebook 장기 토큰 | **60일 — 자동 갱신됨** |
| `FB_APP_ID` / `FB_APP_SECRET` | Meta 개발자 앱 | 없음 |
| `YT_CLIENT_ID` / `YT_CLIENT_SECRET` | Google Cloud OAuth 클라이언트 | 없음 |
| `YT_REFRESH_TOKEN` | OAuth Playground | 사실상 없음 — 단 아래 함정 주의 |
| `GH_PAT` | GitHub Personal Access Token | **직접 정한 만료일 — 주의** |
| `GITHUB_TOKEN` | Actions 가 자동 제공 | 해당 없음 |

### `YT_REFRESH_TOKEN` 의 함정 — Google OAuth 동의 화면 상태

이 토큰은 access_token 과 다릅니다. access_token 은 매번 자동으로 새로 발급되지만,
**refresh_token 자체가 무효화되면 사람이 다시 인증하는 것 말고는 방법이 없습니다.**
가장 흔하고 잘 알려지지 않은 함정: Google Cloud Console 에서 이 OAuth 앱의
**동의 화면 게시 상태가 '테스트'로 남아 있으면 리프레시 토큰이 7일 만에 만료됩니다.**
'프로덕션'으로 전환돼 있어야 계속 유효합니다(현재 정상 게시 중이니 아마 이미
프로덕션 상태일 가능성이 높지만, 한 번 Google Cloud Console → API 및 서비스 →
OAuth 동의 화면에서 확인해두면 안전합니다).

그 외 무효화 원인: 6개월 이상 미사용(매일 게시하므로 해당 없음) / 사용자가 앱
접근을 직접 취소 / 같은 클라이언트의 리프레시 토큰이 50개를 넘어 오래된 것부터
자동 폐기(이 채널은 토큰 1개만 쓰므로 해당 없음).

**자동 감시:** `health-check.yml` 이 매주 이 토큰으로 실제 교환을 시도해
살아있는지 확인합니다. 죽어 있으면 실패 알림이 옵니다 — 그때 OAuth Playground
에서 다시 인증해 새 토큰을 받고 `YT_REFRESH_TOKEN` 시크릿을 교체하세요
(과거에 한 번 했던 절차와 동일합니다).

### Variables (민감하지 않은 설정)

`GEMINI_MODEL`, `TTS_PROVIDER`, `TTS_VOICE`, `IG_API_MODE`, `IG_GRAPH_VERSION`,
`YT_PRIVACY`, `YT_CATEGORY_ID`, `GOOGLE_TTS_VOICE`

> **비용 주의 — `TTS_PROVIDER`**
> 기본값은 무료 edge-tts 입니다. 이 값을 `google` 로 바꾸면 카드가 연결된
> Google TTS 가 호출되어 **요금이 발생합니다.** 건드리지 마세요.

---

## 4. 장기 위험과 자동 감시

사람이 기억해서 챙겨야 하는 일은 안전장치가 못 됩니다.
아래 세 위험은 **`health-check.yml`(🩺 자동화 건강 점검)이 매주 일요일
07:00 KST 에 대신 감시**하고, 문제가 있으면 워크플로를 실패시켜
GitHub 이 알림 메일을 보냅니다. 평소엔 아무것도 안 하셔도 됩니다.

### ① `GH_PAT` 만료 — 가장 위험한 단일 지점

인스타 토큰 자동 갱신(`refresh-token.yml`)이 새 토큰을 시크릿에 써 넣을 때
`GH_PAT` 를 씁니다. PAT 이 만료되면 갱신이 조용히 실패하고, 약 60일 뒤
인스타 게시가 통째로 멈춥니다. 두 달 시차 때문에 원인 찾기가 매우 어렵습니다.

**자동 감시:** 건강 점검이 매주 GitHub API 로 토큰을 실제로 호출해
유효성과 만료일을 확인하고, **만료 21일 전부터 실패 알림**을 보냅니다.
(인스타 토큰이 60일짜리라 그 전에 교체하면 게시가 끊기지 않습니다.)

**알림이 오면:** GitHub Settings → Developer settings → Personal access tokens
에서 새로 발급(권한: 이 저장소의 **Secrets: write**) → `GH_PAT` 시크릿 교체.
아예 만료 없는 토큰으로 발급하면 이 알림이 다시 오지 않습니다.

### ② 저장소 60일 무활동 → 예약 워크플로 자동 정지

GitHub 는 60일간 커밋이 없는 저장소의 cron 을 자동으로 끕니다.
게시할 때마다 `data/history.json` 을 커밋하므로 평소엔 문제없지만,
게시가 오래 실패하면 커밋도 끊겨 예약 실행이 영구히 죽습니다.

**자동 감시 2중:**
- 건강 점검이 매주 `data/heartbeat.txt` 를 커밋합니다. 게시가 전부 멈춰도
  저장소 활동이 남아 **60일 규칙에 걸리지 않습니다.**
- 마지막 게시가 **2일 이상 없으면** 실패 알림을 보냅니다. 게시가 멈춘 사실
  자체를 조기에 알려주므로, 60일까지 갈 일이 없습니다.

### ③ `YT_REFRESH_TOKEN` 무효화 — 자동 재발급이 안 되는 유일한 토큰

자세한 원인은 위 3장의 "`YT_REFRESH_TOKEN` 의 함정" 참고. 죽으면 유튜브
게시만 멈추고(인스타는 계속됨) 사람이 재인증해야 살아납니다.

**자동 감시:** 건강 점검이 매주 실제로 이 토큰을 교환해봅니다. 실패하면
알림이 옵니다 — 실제 게시가 실패하기 전에 미리 알 수 있습니다.

---

## 5. 고장 났을 때 확인 순서

게시가 안 됐다면 이 순서로 봅니다.

1. **Actions 탭 → 해당 실행의 빨간 X 클릭 → 로그 확인.**
   메일로도 실패 알림이 옵니다.
2. 로그에서 이 표시들을 찾습니다.

| 로그에 보이는 것 | 뜻 | 대응 |
| --- | --- | --- |
| `❌ 인스타 게시 실패` | 인스타만 실패 (유튜브는 성공) | 토큰 갱신 워크플로 상태 확인 |
| `❌ 유튜브 게시 실패` | 유튜브만 실패 | 할당량 초과인지, 토큰 문제인지 메시지 확인 |
| `Gemini 전 후보 혼잡/한도` | 무료 일일 한도 소진 | 몇 시간 뒤 자동 회복. 잦으면 게시 횟수를 줄인다 |
| `⚠️ 115초 초과` / `85초 미만` | 러닝타임이 목표 밖 | 아래 6번의 분량 기준 조정 |
| `⚠️ 재생성에도 중복` | 소재가 겹침 | 대개 그냥 넘어가도 됨 |
| `Pexels ... 폴백` 다수 | 자료화면 확보 실패 | 검정 배경으로 나감. 잦으면 검색어 규칙 손질 |

3. **일단 멈추고 싶으면** `⏸️ 자동 게시 멈추기` 를 실행하세요.
   고친 뒤 `▶️ 자동 게시 다시 시작` 으로 되돌립니다.
4. **고친 뒤 검증**은 반드시 `post-reel.yml` 을 `dry_run = true` 로 돌려
   Artifacts 의 영상을 확인하고 나서 실제 게시를 재개하세요.

### 유튜브 할당량

`videos.insert` 는 하루 100회 전용, 나머지는 10,000 유닛 공용입니다.
게시 1건당 썸네일(50) + 자막끄기(400) = 450 유닛이라 **하루 22건이 기술적 상한**입니다.
현재 3건이므로 여유가 큽니다. 태평양 시간 자정에 리셋됩니다.

---

## 6. 자주 손보게 되는 설정값

| 바꾸고 싶은 것 | 파일 | 위치 |
| --- | --- | --- |
| 게시 시각·횟수 | `.github/workflows/post-reel.yml` | 맨 위 `cron:` 세 줄 |
| 랜덤 지연 폭 | 같은 파일 | `Random delay` 스텝의 `% 7201` (초) |
| 영상 길이 | `src/assistants/producer.ts` | `MIN_CHARS` / `IDEAL_CHARS` / `MAX_CHARS` |
| 소재 각도 비중 | `src/lib/variety.ts` | `TOPIC_ANGLES` 의 `weight` |
| 지역 비중 (한국 비율 등) | 같은 파일 | `REGION_ANGLES` 의 `weight` |
| 오프닝 훅 종류 | 같은 파일 | `HOOK_STYLES` |
| 마무리 멘트 종류 | 같은 파일 | `SIGNOFF_STYLES` |
| 목소리·말 속도 | 같은 파일 | `VOICES` 의 `rate` / `pitch` |
| 자막 디자인 (매트 패널·라벨) | `src/remotion/MysteryReel.tsx` | `Caption` / `PANEL_LABELS` |
| 전환·자막 모션 타이밍 | 같은 파일 | 맨 위 `DISSOLVE_FRAMES` / `CAPTION_*` 상수 |
| BGM 볼륨·반전 딥 깊이 | 같은 파일 | `BGM_VOLUME` / `BGM_DIP_VOLUME` |
| 장르별 색보정·강조색 | `src/lib/grade.ts` | `LOOKS` (장르 4종) / `detectGenre` |
| 장면당 컷 수 상한 | `src/lib/scenes.ts` | `MAX_SHOTS_PER_SCENE` |
| 자막 스타일 비중 (매트 패널 70%) | `src/lib/variety.ts` | `BOX_STYLES` 의 `weight` |
| 대본 규칙 전반 | `src/assistants/producer.ts` | `system` 배열 (프롬프트) |

**연출 구조(2026-08 디자인 개편):** 사진은 컷이 아니라 **장면(2~3컷)** 단위로
1장을 받아 크롭·줌 변주로 나눠 씁니다(LLM 이 `scene` 번호 부여 →
`scenes.ts` 정규화 → `broll.ts` 장면당 1장). 같은 장면 안은 6프레임
디졸브, 장면 경계는 하드컷, 반전(reveal)은 하드컷 + 펀치 줌 + BGM 딥
(영상당 최대 2회). 색보정은 랜덤이 아니라 소재 장르(coldCase/court/
history/folklore)가 정하고, 사건별로 ±4% 지터만 섞입니다.

**영상 길이 환산 기준(실측 7건):** 순수 발화 속도는 보이스마다 다릅니다 —
Hyunsu 저음 **8.66자/초**(가장 느림), Hyunsu 기본 9.13, InJoon 9.68.
여기에 세그먼트당 약 0.2초 호흡이 더해집니다. 가장 느린 보이스 기준:
940자 + 29컷 ≈ 114초, 900자 ≈ 110초, 800자 ≈ 98초.

> **상한(`MAX_CHARS`)은 목표치가 아니라 재생성 트리거입니다.**
> 모델이 실제로 만들어내는 분량(실측 평균 909자)보다 상한을 낮게 잡으면
> 매번 재생성이 돌아 Gemini 무료 호출을 2~4배로 태웁니다. 실제로 상한이
> 900 이던 동안 7회 중 5회가 재생성됐습니다. 지금은 프롬프트 목표를
> 850~900 으로 두고 상한은 940 으로 여유를 줍니다.

길이를 바꿀 때 `producer.ts` 의 상수와 프롬프트 문구를 **둘 다** 고쳐야
합니다. 한쪽만 고치면 재생성이 헛돕니다.

수정 후에는 `npx tsc --noEmit` 으로 타입 검사를 하고, `dry_run` 으로 확인하세요.

---

## 7. 지켜야 하는 원칙 (지난 사고에서 나온 것들)

- **비용 0원.** 유료 API 를 붙이지 않습니다. 특히 `TTS_PROVIDER=google` 금지.
- **연령제한 표현 차단.** 자살·자해·노골적 성 표현은 `src/lib/safeText.ts` 가
  탐지·중립화합니다. 한 번 연령제한이 걸리면 도달이 크게 떨어집니다.
- **한국 소재는 안전 유형만.** 전설·수백 년 전 역사·30년 이상 지난 공적 사건·
  확정 판결 사건. 한국은 사실을 말해도 명예훼손이 성립하므로(형법 307조 1항)
  실명·정확한 지역명·최근 5년 내 사건은 금지입니다.
- **진상이 밝혀진 사건에 '미제' 표기 금지.** 해명이 있으면 후반 반전으로 넣습니다.
- **캡션에 해시태그 금지**, 한국어 위 / 영어 아래.
- **'팔로우·구독·채널' 금지** — 인스타·유튜브 동시 게시라 '좋아요·댓글'만 씁니다.

---

## 8. 성과 확인

- `📊 조회수·반응 리포트` 워크플로를 수동 실행하면 요약이 나옵니다.
- 유튜브 스튜디오 → 분석 → **평균 시청 지속 시간**을 봅니다.
  현재 영상 길이를 50초대에서 100초대로 바꾼 직후라, 두 그룹을 비교해
  100초 쪽 지속 시간이 더 짧으면 길이를 되돌리는 편이 낫습니다.
- 시청자층(연령·지역)이 바뀌면 `producer.ts` 의 `[★주 시청자층]` 블록의
  수치도 함께 갱신하세요. 소재 선택이 그 숫자를 기준으로 돌아갑니다.

---

## 9. 5년 무인 운영을 위한 자동 방어 장치

이 채널은 사람 개입 없이 오래 돌아가는 게 목적이라, 시간이 지나면 반드시
깨지는 두 지점에 자동 우회 로직을 넣었습니다.

### edge-tts 장애 대응 (`src/assistants/narrator.ts`)

edge-tts 는 Microsoft 공식 API 가 아니라 Edge 브라우저 읽어주기 기능을
역공학한 비공식 라이브러리라, Microsoft 가 엔드포인트나 인증을 바꾸면
예고 없이 깨질 수 있습니다. 나레이션이 죽으면 게시 전체가 실패합니다.

- 실패 시 최대 3회 재시도(2초·4초·6초 간격) — 일시적 네트워크 문제 대응
- 3회 모두 실패하면 **Google 번역의 비공식 무료 TTS**로 자동 전환
  (문장 단위로 쪼개 합성 후 ffmpeg 로 이어붙임). 품질은 떨어지지만
  게시가 아예 끊기는 것보다 낫습니다.
- 로그에 `⚠️ edge-tts N회 연속 실패 — 대체 TTS로 전환합니다`가 보이면
  edge-tts 쪽에 문제가 생겼다는 신호입니다. 자주 보이면 edge-tts 라이브러리
  업데이트가 필요할 수 있습니다(`pip install -U edge-tts`).

### 인스타 Graph API 버전 만료 대응 (`src/lib/igGraphVersion.ts`)

메타는 Graph API 버전을 약 2년마다 지원 종료합니다. 5년이면 최소 2~3번
버전이 바뀌는데, 코드에 고정해두면 그때마다 인스타 게시가 멈춥니다.

- 요청이 "버전 지원 종료" 오류로 실패하면 자동으로 다음 버전
  (`v21.0` → `v22.0` …)을 순서대로 시도합니다.
- 성공한 버전은 `data/igGraphVersion.txt` 에 기록되고, 게시 워크플로가
  `data/history.json` 과 함께 자동 커밋합니다. **다음 실행부터는 시행착오
  없이 그 버전으로 바로 시작**합니다.
- 이 파일이 새로 생기거나 바뀌었다면 메타가 실제로 예전 버전을 지원
  종료했다는 뜻입니다. 특별히 손볼 것은 없지만, `.env` 나 `IG_GRAPH_VERSION`
  변수를 수동으로 건드리지 않았다면 이 파일이 우선 적용됩니다.

### 이 장치들로도 못 막는 것

- **YouTube API(v3)**: 구글이 수년째 버전을 올리지 않아 별도 장치를 두지
  않았습니다. 만약 v4 가 나오면 그때는 코드 수정이 필요합니다.
- **플랫폼 정책 변화**: 유튜브의 AI 양산 콘텐츠 단속처럼 코드로 막을 수
  없는 리스크는 여전히 존재합니다. `health-check.yml` 이 기술적 장애는
  잡아내지만 정책 위반으로 인한 계정 제재까지는 감지하지 못합니다.
